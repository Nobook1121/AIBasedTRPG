{
  "name": "aibased-trpg",
  "version": "0.3.4",
  "private": true,
  "type": "module",
  "scripts": {
    "prepare:frontend": "node scripts/build-frontend.mjs --assets-only",
    "typecheck": "npm run prepare:frontend && tsc --noEmit",
    "build:frontend": "npm run prepare:frontend && tsc --project tsconfig.frontend.json"
  },
  "devDependencies": {
    "typescript": "^5.8.3"
  }
}
